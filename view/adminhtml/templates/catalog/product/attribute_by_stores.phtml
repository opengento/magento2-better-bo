<?php
    
    $productId = $block->getRequest()->getParam('id');
    $bearer = $block->getCookie('betterbo_token');

    $user = $block->getAuth()->getUser();

    $extraData = json_decode($user->getExtra(), true) ?: [];
    $bearer = $extraData['betterbo_token'];

?>

<div 
    id="product_attributes"
    data-product-id="<?= $productId ?>"
    data-bearer="<?= $bearer ?>"
></div>